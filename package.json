{
  "name": "chai-samsam",
  "version": "0.0.1",
  "description": "Deep matching chai assertions using samsam",
  "main": "index.js",
  "scripts": {
    "lint": "standard",
    "test": "npm run lint && mocha",
    "cover": "istanbul cover _mocha",
    "coverage": "npm run cover && codecov && rm -rf ./coverage"
  },
  "keywords": [
    "chai",
    "chai-plugin",
    "samsam",
    "deep-match",
    "chai-deep-match"
  ],
  "author": "Tim Beyer",
  "license": "MIT",
  "dependencies": {
    "samsam": "^1.2.1"
  },
  "devDependencies": {
    "chai": "^4.1.0",
    "codecov": "^2.2.0",
    "istanbul": "^0.4.5",
    "mocha": "^3.4.2",
    "standard": "^10.0.2"
  }
}
